# --- Database (libSQL / Turso) ---
# Local development: a plain SQLite file (NOT committed, NOT used on Vercel).
DATABASE_URL="file:./local.db"

# Production (Vercel): point at your Turso database.
# DATABASE_URL="libsql://<your-db>-<org>.turso.io"
# DATABASE_AUTH_TOKEN="<your-turso-auth-token>"
DATABASE_AUTH_TOKEN=""

# --- Edit login ---
# Seeded into the `users` table by `npm run db:seed`. Login validates against
# the DB (passwords are stored hashed, never plaintext). AUTH_SECRET signs the
# session cookie and the password hash — keep it secret and stable in prod.
APP_USERNAME="admin"
APP_PASSWORD="change-me"
AUTH_SECRET="generate-a-long-random-string"
